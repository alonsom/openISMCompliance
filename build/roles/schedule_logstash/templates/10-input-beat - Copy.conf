input {
  beats {
    port => "5044"
#    ssl => true
#     ssl_certificate => "{{ssl_certificate_folder}}/logstash-forwarder.crt"
#    ssl_key => "{{ssl_key_folder}}/logstash-forwarder.key"
  }
}